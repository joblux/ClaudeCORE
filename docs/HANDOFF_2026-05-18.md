# JOBLUX HANDOFF — 2026-05-18

Tactical session continuity only. Doctrine lives in `docs/JOBLUX_STATE.md` (supreme) and `docs/PROFILUX_MATRIX_V1.md` (ProfiLux contract). On any conflict, STATE wins.

Repo: `joblux/ClaudeCORE` @ `main` HEAD `29ae0789`. Local: `/Users/momo/Documents/GitHub/ClaudeCORE`. Supabase: `zspcmvdoqhvrcdynlriz` (eu-west-1).

---

## SHIPPED THIS SESSION

**Docs-only reconciliation. Zero code change.**

V3.4 References had shipped 2026-05-17 21:10 UTC at commit `29ae0789` but the previous session closed before STATE was rotated. STATE through `c61e352` (V3.3) carried two false claims:
1. Pack D was "6/8 active" — actually 7/8 since 29ae0789.
2. References was "DDL-blocked alongside Internships" — actually `members."references"` jsonb column exists; orphan May 16 MCP migration created 5/6 jsonb columns, not 4/6.

Today's session reconciled docs to HEAD. No new substrate, no new code, no new MATRIX edits.

---

## QA EVIDENCE (2026-05-18 AM)

- **Substrate** — `members."references"` jsonb confirmed via Supabase MCP. 1 row populated on Alex (`49542211`): `{name:"Marie Dubois", role:"Global Retail Director", company:"Chanel"}` — exact match to locked `ProfiLuxReferenceItem` contract. Row written 2026-05-17 21:15:33 UTC (5min post-ship of 29ae0789).
- **Code wiring** — Verified at HEAD: `lib/profilux/types.ts` (`ProfiLuxReferenceItem` + `MemberRow.references` + `ProfiLuxResolved.references` + `EditorView.references`), `lib/profilux/resolveProfiLux.ts` (`row.references ?? []` passthrough line 351), `lib/profilux/projectFor.ts` (EditorView line 298), `app/api/profilux/route.ts` (`coerceReferenceItem` validator + `has('references')` write block), `app/dashboard/candidate/profilux/page.tsx` (`ADD_SECTION_LIBRARY` available:true line 29, `refsDraftRows` state, save handler, full Edit UI section ~line 5024).
- **Live QA on Alex** — 4/4 PASS:
  1. References SectionCard renders in Edit tab with Marie Dubois row visible.
  2. Edit on row → modify field → save: persisted.
  3. F5 reload: change still present.
  4. No regression on Awards / Certifications / Memberships / Strategic Initiatives / Portfolio / Press & Features.

---

## UNRESOLVED / OPEN

- **STATE drift discovery.** The 5/6 vs 4/6 orphan-migration miscount in STATE pre-c61e352 should serve as caution: when a "DDL-blocked" claim is carried forward across sessions, verify against `information_schema.columns` at session open before scoping work. Today's audit caught this in 1 query and saved a wasted DDL slice.
- **`members.internships` genuinely missing.** Confirmed via `information_schema.columns`. V3.5 Internships remains the only true DDL-blocked Pack D slice. Shape + required-field decision still pending.
- **Pre-existing Marie Dubois reference row provenance.** Likely manual SQL seed during V3.4 first-ship live test on 2026-05-17 21:15 UTC. Not CV-parsed (CV parser does not currently write `references`). Low-risk, single fixture, ignore unless audit needed.
- **Carry-overs from HANDOFF_2026-05-17.md still open:** `profilux` table DDL drop (ledger `6aef236e`); Supabase PITR window vs `/privacy` 90-day commitment (platform-ops); `F-opportunity-preferences-ghost-endpoint` (`7439d0a7`, parked).

---

## NEXT STRICT STEP

**Lane open — awaiting Mo direction.** Pack D Phase 2 jsonb track closed at 7/8. Remaining track candidates:

1. **PF-D V3.5 Internships** — DDL + shape decision required first. No code slice may start before column shape lock (jsonb vs text[], required fields, Emerging-user surface posture).
2. **G7 candidate-side recruiting feed.**
3. **Other** — explicit Mo choice.

**Workflow doctrine (reconfirmed):** Claude AI = audit / scope / QA. GPT = final compact Claude Code prompt. Claude Code = execution. Claude AI does NOT draft Claude Code prompts directly.

---

## SESSION-OPEN CHECKLIST (next session)

1. Read `docs/JOBLUX_STATE.md` first.
2. `git log --oneline -5` (Claude Code) — expect `29ae0789` as HEAD or later if reconciliation pushed.
3. Verify STATE.LAST SHIPPED top entry matches HEAD commit SHA (today's reconciliation closes a 1-commit drift).
4. If next step still "PF-D V3.5 Internships" or "open lane", proceed per Mo direction.
5. Reflex check before any Pack D slice: query `information_schema.columns` for the target `members.<col>` to verify substrate state before scoping (lesson learned today).

---

*Tactical handoff only. Doctrine, history, and full ledger reconciliation live in STATE.*

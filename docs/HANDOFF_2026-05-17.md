# JOBLUX HANDOFF — 2026-05-17

Tactical session continuity only. Doctrine lives in `docs/JOBLUX_STATE.md` (supreme) and `docs/PROFILUX_MATRIX_V1.md` (ProfiLux contract). On any conflict, STATE wins.

Repo: `joblux/ClaudeCORE` @ `main` HEAD `1b4b8c3` (post-DDL). Local: `/Users/momo/Documents/GitHub/ClaudeCORE`. Supabase: `zspcmvdoqhvrcdynlriz` (eu-west-1).

---

## SHIPPED THIS SESSION

**1. Pack B.1.4 — Legacy share cleanup** · `a5a4bc2`
- Closing slice of Pack B.1. Subtractive removal of `profilux.share_slug` + `profilux.sharing_enabled` reads/writes across 3 files (share route GET Path B + POST legacy branch + mirror write; reset-link slug uniqueness + trailing upsert; `[slug]/page.tsx` Path B fallback). Error codes consolidated to `NO_SHARE_LINK` 400. 3 files, +38 / -147.
- 9/9 Chrome MCP live tests PASS (active render, disabled 404, password gate redirect, expired redirect, nonexistent 404, GET empty/populated shapes, POST `NO_SHARE_LINK`, sharing toggle round-trip, rotation w/ `rotated_from` persisted).
- `profilux.updated_at` invariant UNCHANGED at `2026-05-12 14:03:20` throughout QA → mirror writes confirmed removed at runtime. `share_links` is sole runtime source of truth.
- DB column drop still pending (parent ledger `6aef236e` open).
- Ledger ops: `8b369d80` (Pack B.1) label-appended ` + a5a4bc2 (B.1.4)` + notes; `6aef236e` (profilux retire) notes annotated with code-side closure.

**2. Pack B.3.1 — Member lifecycle substrate** · `ccc8a6d` (docs) + `1b4b8c3` (DDL)
- **B.3.1a** (`ccc8a6d`, docs-only): MATRIX v1.7 — new §25 Member lifecycle (10 subsections); §20.x rewrite (forward dep on B.3.3, caption decision deferred); §13 +3 deferred rows; footer to v1.7. STATE — §12 lifecycle paragraph, §24 hard-launch bullet, DO NOT +3 bullets. 2 files, +131 / -3. Mo's §25.8 erasure-posture wording applied verbatim.
- **B.3.1b** (`1b4b8c3`, DDL): `supabase/migrations/20260517_member_soft_delete.sql` (35 lines). **Migration applied in prod** via Supabase MCP — version `20260516154744`.
- Substrate landed: `members.deleted_at TIMESTAMPTZ NULL` + `members.deleted_by UUID NULL`; `idx_members_deleted_at_partial` (WHERE deleted_at IS NOT NULL); `idx_members_email_active` partial unique on `lower(email)` WHERE `deleted_at IS NULL`; `members_email_key` UNIQUE dropped; `idx_members_email` non-unique btree preserved.
- RLS Posture B (defended, not half-open): `Members read own` USING `(auth_user_id = auth.uid() AND deleted_at IS NULL)`; `Members update own` same USING + matching WITH CHECK. Admin + service-role policies untouched per §25.3.
- Baseline: 13/13 active, 0 soft-deleted. **Zero runtime behavior change** — no app code reads `deleted_at` yet.
- Ledger row `1be7a829-61af-405d-9ba2-7d1ba970eb5c` (closed/normal/Substrate).

**Sibling parked finding** · ledger `7439d0a7-94b1-4820-baf4-2a570469f00a`
- F-opportunity-preferences-ghost-endpoint. `/api/members/preferences` route targets `opportunity_preferences` table which does not exist in DB. Discovered during B.3 audit. Parked/low/Cleanup. No fix scheduled.

---

## UNRESOLVED / OPEN

- **Substrate ≠ behavior.** B.3.1b is DB-defended only. `members.deleted_at` exists, RLS enforces it, but `resolveProfiLux` does not yet check it. Until B.3.2 wires the guard, soft-deleting a row by hand (`UPDATE members SET deleted_at = now() WHERE id = ...`) would: (a) correctly block auth-user own-reads via tightened RLS, (b) NOT hide the profile from public `/[slug]` or admin reads (both bypass RLS via service role). Do not test soft-delete on real members until B.3.2 ships.
- **Caption decision pending.** View tab LEFT SPINE "Visible to JOBLUX matching only" wording remains PROVISIONAL per MATRIX §20.x. Decision (gate on `matching_opt_in = true` vs remove entirely) deferred to B.3.3 execution.
- **Privacy page operational reconciliation.** `/privacy` publicly commits to 30-day deletion + 90-day backup retention. Supabase PITR window for current tier should be verified to match this commitment before B.3.2 ships the delete endpoint. Platform-ops scope, not doctrine.
- **Settings page is greenfield.** No `/dashboard/candidate/settings` route exists. B.3.2 will create it from scratch. Doctrinally locked per MATRIX §19 / §21.3 — NOT a ProfiLux Manage extension.
- **`profilux` table retire** still tracked at ledger `6aef236e` (open/low). B.1.4 closed all code-side dependencies on `share_slug` + `sharing_enabled`; DDL drop remains pending — separate slice, not in B.3 scope.

---

## NEXT STRICT STEP

**Pack B.3.2 — Member lifecycle write path.** Scope locked, awaiting Mo direction to start.

Recommended micro-slice ordering (to be confirmed at session open):

1. **B.3.2.a — Resolver guard.** Single-file edit in `lib/profilux/resolveProfiLux.ts`: return `null` when `members.deleted_at IS NOT NULL`. Affects all 6 surfaces via `projectFor`. Lowest-risk, foundational, no UI.
2. **B.3.2.b — `/api/members/delete` endpoint.** Auth-required POST. Atomic: sets `deleted_at = now()` + `deleted_by = <self>`, forces `share_links.sharing_enabled = false`, hard-deletes `nextauth_accounts` rows for the member, revokes active session.
3. **B.3.2.c — Admin variant.** `/api/admin/members/[id]/delete` with `deleted_by = <admin_id>`; restore endpoint clears `deleted_at`. Admin Trash UI: filter `/admin/members?status=deleted` (read existing admin members list) + restore button.
4. **B.3.2.d — Settings page scaffold.** Greenfield `/dashboard/candidate/settings/page.tsx` + matching API. Phase 1 hosts only the delete-account control (with confirmation modal per MATRIX §14.2 / §24.2 — destructive action). Phase 2 (post-B.3.3) adds `matching_opt_in` toggle. Phase 3 (post-B.3.4) adds RGPD export trigger.

**Hard gates before B.3.2 starts:**
- Mo green-light on the 4-slice ordering above (vs alternative grouping).
- Confirm Supabase PITR window matches `/privacy` 90-day commitment (verify against Supabase dashboard before delete endpoint ships).
- No mid-slice scope expansion. Each micro-slice = one PR, propose → approve → execute.

**Workflow doctrine (locked 2026-05-16, reconfirmed):** Claude AI = audit / scope / QA. GPT = final compact Claude Code prompt. Claude Code = execution. Claude AI does NOT draft Claude Code prompts directly.

---

## SESSION-OPEN CHECKLIST (next session)

1. Read `docs/JOBLUX_STATE.md` first.
2. `git status` (Claude Code) — expect clean working tree.
3. `git log --oneline -5` — expect this close commit as HEAD.
4. Supabase MCP — verify migration `20260516154744` still present + verify `members.deleted_at` column still exists (defensive check, should never fail).
5. `admin_tasks` MCP read — confirm row `1be7a829` still closed, row `7439d0a7` still parked.
6. Reconcile CURRENT STEP — if next step still "Pack B.3.2", proceed; if Mo pivots, surface conflict before any work.

---

*Tactical handoff only. Doctrine, history, and full ledger reconciliation live in STATE.*

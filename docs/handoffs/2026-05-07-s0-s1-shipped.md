# JOBLUX HANDOFF — 2026-05-07 PM

**Session theme:** ProfiLux Reload — S0 docs + S1 CV pipeline shipped + prod-validated
**Author:** Claude AI (Opus) · JOBLUX Ops
**Closes session opened:** 2026-05-07 PM (continuation of morning Phase 3 closure)

---

## SHIPPED THIS SESSION

| # | Commit | Type | What | Status |
|---|---|---|---|---|
| 1 | `5d8672b` | docs | MATRIX v1.2 — promote UX MAP triad + sections + responsive + components | ✅ Pushed `origin/main`, MCP-verified |
| 2 | `b533e31` | feat | S1 additive CV upload + parse card (no prefill) | ✅ Pushed `origin/main`, MCP-verified, prod-validated |

Commit chain on `origin/main`:
```
b533e31 feat(profilux): S1 additive CV upload + parse card (no prefill)
5d8672b docs(profilux): MATRIX v1.2 - promote UX MAP triad + sections + responsive + components
d8eb3ef docs(state): rotate ACTIVE CHAIN - ATS Surface 3 SHIPPED, Phase 3 CLOSED
```

---

## S0 — MATRIX v1.2 doctrine patch

**Commit:** `5d8672b`
**File:** `docs/PROFILUX_MATRIX_V1.md` (+188 / -2)
**Remote SHA after push:** `6596b2a8c3a5977dac9af3d8c691634baa61445d`

**What it locks:**

- **§21** View / Edit / Manage triad — three named modes; user always in one and only one. View = default landing (passport read view + state markers + drawer triggers). Edit = section-scoped drawer, never page-scoped, no global edit toggle. Manage = configuration scope (public URL toggle, maskable toggles, export, account prefs); same conceptual surface as §19 Settings.
- **§22** Section catalog — 9 default sections in fixed order (Identity / Current Position / Luxury Fit / Career History / Education & Languages / Skills & Markets / Clienteling / Availability & Targets / Compensation), all permanent, field assignments mirror §7.6.1 EditorView exactly. Plus 8 add-library Tier 2 sections (Certifications, Awards, References, Portfolio, Publications, Memberships, Speaking, Volunteer) — opt-in, schema PARKED until Tier 2 work.
- **§23** Responsive philosophy — desktop primary (identity strip top-fixed + section card grid), mobile stacking single-column with full-viewport drawers. Mobile hierarchy: Identity → Current Position → Luxury Fit highest priority.
- **§24** Component family strategy — family-level only, no specific component names locked: section card, drawer, state marker (visual cues only — MUST NOT block save / prevent matching / escalate to modals / change layout), chip multi-toggle, tri-state Yes/No (with locked clear-on-transition rule), identity strip.
- CHANGE LOG entry v1.2.
- §22.1 Identity row extended with `bio` + `linkedin_url` (GPT micro-correction).
- §23.5 mobile hierarchy phrasing tightened (GPT micro-correction).

**What it does NOT touch:** §§1–20, §7.6.1 EditorView shape, §4.5 W1/W2/W3 write contract, §6 resolver, §7 projection masks, §10 utilities, §13 deferred items list. Substrate fully preserved.

**§13 deferred items partially closed:** Triad / section catalog / responsive philosophy / component families now repo-locked here; their previous "pending MATRIX promotion" labels in `docs/PROFILUX_RELOAD_UX_MAP.md` are superseded by §§21–24.

**Workflow note for next session:** GitHub MCP `create_or_update_file` returned **403 "Resource not accessible by integration"** when attempting to write the patched file directly. Fallback pattern adopted (Option B):

1. File generated in `/mnt/user-data/outputs/PROFILUX_MATRIX_V1.md` by Claude AI
2. Mo downloads to `~/Downloads/`
3. Claude Code runs single line: `cd ... && cp ~/Downloads/PROFILUX_MATRIX_V1.md docs/PROFILUX_MATRIX_V1.md && shasum docs/... && wc -l docs/... && tail -3 docs/... && git diff --stat docs/...`
4. After 5 verification checks pass, separate Code line: `git add ... && git commit -m "..." && git push origin main`
5. GitHub MCP `list_commits` + `get_file_contents` immediately after push to confirm distant SHA matches and content survived

Lock this pattern for any large doc edit where MCP write surface is unreliable. **Do not use heredoc for large files** — too risky, can corrupt MATRIX if a single character drops.

---

## S1 — additive CV upload + parse card

**Commit:** `b533e31`
**File:** `app/dashboard/candidate/profilux/page.tsx` (+167 / -1)

**GPT decisions locked pre-implementation:**
- **G1 = γ:** No identity prefill in this slice. Direct POST to `/api/profilux` from parse handler would violate the no-silent-L1-to-L2 invariant. Making Screen 1 editable is too large for S1. Prefill becomes a separate S1.5 slice.
- **G8 = above-renderStep, full-width:** Card placed immediately before `{renderStep()}` in the JSX. Style matches existing tunnel cards (dark `#222`, border `#2a2a2a`, `maxWidth: 900` consistent with the 7 write-enabled screens 3/4/6/7/8/9/10 + `grid` const).
- **G2 = source CV state from `editor.cv_meta` only:** Drop the `/api/members/me` second fetch from historical commit `408cd7d`. `cv_meta` already on `/api/profilux` GET response — single source, no drift.

**Implementation surface:**

1. `useRef` added to existing top React import line.
2. 5 new `useState` hooks + 1 `useRef` inside `ProfiluxPage`: `fileInputRef`, `uploading`, `parsing`, `uploadError`, `parseError`, `needsReviewCount`.
3. Top-level `mapParseError(code)` helper outside component, mapping 10 `M6_*` error codes to short user-facing strings + default fallback.
4. 3 handlers between existing `refetch` and `useEffect`:
   - `handleUploadClick` — clears `uploadError`, triggers hidden file input
   - `handleFileSelected` — POST `multipart/form-data` to `/api/members/cv-upload` with field name `cv`. On success: `await refetch()` to refresh `editor.cv_meta`; reset `needsReviewCount` to null. On failure: `setUploadError`.
   - `handleParse` — POST to `/api/members/cv-parse` with no body. On success: `await refetch()` + `setNeedsReviewCount` from `data.needs_review_count`. On failure: `setParseError(mapParseError(data?.error))`.
5. Derived values added after `const e = editor`: `cvUrl`, `cvParsedAt`, `parsedDateLabel`.
6. JSX card inserted immediately before `{renderStep()}`. Three visual states keyed off `(cvUrl, cvParsedAt)`:
   - **Cas A** (no cvUrl): "Upload your CV. JOBLUX will parse it for review." + primary "Upload CV" button
   - **Cas B** (cvUrl, no cvParsedAt): "CV uploaded. [Replace]" + primary "Parse CV" button
   - **Cas C** (both set): "CV parsed `<date>`. [Replace]" + secondary "Re-parse" button
   - Inline error rendering for `parseError || uploadError` (red `#ff6b6b`)
   - Inline `needsReviewCount` line when > 0 (gray `#888`)
   - Hidden `<input type=file accept=".pdf,.docx">`
   - Reuses existing `saveBtn` / `saveBtnDis` styles

**Doctrine compliance verified post-implementation:**
- Zero new `fetch('/api/profilux'` calls in diff (the one match in baseline grep was the existing `refetch` GET, unmodified)
- Zero `applyCvPrefill` references (deferred to S1.5 per G1=γ)
- Zero new `case N:` arms in `renderStep()` switch
- Zero changes to the 7 draft state quartets
- Zero changes to `refetch` body / Prev-Next nav / any existing handler
- `cv-upload`, `cv-parse`, `profilux` route, `lib/profilux/types.ts` all read-only — not touched
- TSC clean (`npx tsc --noEmit` exit 0)
- Build clean (`npm run build` ✓ Compiled successfully)

---

## S1 PROD VALIDATION

End-to-end on Alex Mason fixture (`luxuryretailsale@gmail.com`, member id `e6899932-e5ab-49a5-9ced-dc404fad87b1`).

**UI walk:**
1. Reload `joblux.com/dashboard/candidate/profilux` — page loads in ~3s, shows "ProfiLux — Identity Screen 1/11"
2. CV card visible above tunnel as designed: "CV / CV uploaded. Replace / [Parse CV]" — exactly cas B
3. Click "Parse CV" → button immediately shows "Parsing..." (state propagates correctly)
4. ~28s Haiku roundtrip
5. Card transitions to cas C: "CV parsed 5/7/2026. Replace / [Re-parse]" + "**7 items to review**"
6. No error, no spinner stuck, no layout shift, no console errors

**DB validation (Supabase MCP, baseline at 21:15:20 UTC vs post-parse at 21:19:03 UTC):**

| Field | Baseline | Post-parse | Verdict |
|---|---|---|---|
| `cv_url` | uploaded (`MOBANYC.pdf`) | unchanged | ✓ |
| `cv_parsed_at` | **null** | **2026-05-07 21:19:03 UTC** | ✓ L1 written |
| `cv_parsed_data` | empty | **populated** (3 experiences, 7 needs_review) | ✓ L1 written |
| `needsReviewCount` (UI) | — | **7** | ✓ exact match DB `jsonb_array_length(cv_parsed_data->'needs_review')` |
| `first_name` (L2) | Alex | **Alex** | ✓ unchanged |
| `last_name` (L2) | Mason | **Mason** | ✓ unchanged |
| `city` (L2) | Paris | **Paris** | ✓ unchanged |
| `country` (L2) | Andorra | **Andorra** | ✓ unchanged |
| `nationality` (L2) | null | **null** | ✓ unchanged |
| `phone` (L2) | +33 970469633 | **+33 970469633** | ✓ unchanged |
| `headline` (L2) | null | **null** | ✓ unchanged |
| `bio` (L2) | null | **null** | ✓ unchanged |
| `job_title` (L2) | null | **null** | ✓ unchanged |
| `current_employer` (L2) | null | **null** | ✓ unchanged |
| `profile_completeness` (L3) | 0 | **0** | ✓ recompute ran (W3) but L1 alone doesn't move score, consistent with backend-only readiness signal |

**Doctrine proof — L2 sovereignty under L1 parse:**

The CV file `MOBANYC.pdf` parsed by Haiku contained identity for a different person:
- L1 result: `cv_parsed_data->'identity'->>'first_name' = 'Alex'`, `last_name = 'Mazour'`, `city = 'NYC'`
- L2 actual at the same moment: `first_name = 'Alex'`, `last_name = 'Mason'`, `city = 'Paris'`

If the system had ever written L1 → L2 silently, the L2 row would have been corrupted from "Mason / Paris" to "Mazour / NYC". **It did not.** The system perfectly isolated the two layers. Mason / Paris remained authoritative. This is exactly the doctrine behavior locked in MATRIX v1.2 §5.2 (L1 may suggest/prefill L2 only via explicit user confirmation; never silently). S1 ships proof of compliance under live conditions.

---

## NEXT STRICT STEP

**S1.5 — planning only. No code.**

Identity edit / explicit prefill decision. Mo approves the planning output before any implementation prompt is drafted for Code.

**S1.5 scope to be planned:**
- Decide identity prefill mechanism: explicit user-confirmed merge UX (review screen with diff + accept/reject per field) versus making Screen 1 write-enabled with prefill suggestions surfaced as inline "Apply from CV?" affordances.
- Constraint: must respect MATRIX v1.2 §5.2 + §17 (L1 may suggest/prefill L2 only via explicit user confirmation; no silent writes).
- Constraint: must align with §22.1 default Identity section field list (`first_name, last_name, city, country, nationality, phone, headline, avatar_url, bio, linkedin_url`) — but minimum viable S1.5 scope is the 4 fields `408cd7d` originally targeted (`first_name, last_name, city, nationality`).
- Out of scope for S1.5: passport / drawer cutover (separate later slice), Settings / Manage surface (PARKED per §19/§21.3), Tier 1 schema (PARKED per §15.2), Tier 2 add-library sections (PARKED per §22.2), maskable layer schema (PARKED per §16.4), CV merge state machine API endpoints (PARKED per §17.3 — prefill UI sits on top of existing routes only).

---

## GUARDRAILS LOCKED FOR S1.5 SESSION

- **No** silent POST to `/api/profilux` from any parse handler
- **No** `cv-parse` route changes (currently green prod, STATE DO NOT explicit, S1 reconfirmed)
- **No** schema migrations
- **No** Tier 1 columns added
- **No** Settings / Manage surface built
- **No** passport / drawer cutover
- **No** removal of any of the 11 existing tunnel screens
- The §4.5 W1/W2/W3 write contract applies unchanged to any new identity write path

---

## LEDGER STATE AT SESSION CLOSE

Reconciliation queried via Supabase MCP at 21:21 UTC. No DB writes needed this session.

- `8f82b3ac` Phase 4 — premium ProfiLux tunnel + editor rebuild → **`validated`** priority `high` (correct as anchor; carries through tunnel passport rewrite without DB rotation)
- `88d4bd79` ProfiLux Reload — canonical living-object model + animation prototype → **`open`** priority `high` (correct umbrella for S1.5 → passport work)
- `9da90f08` F-public-share-currently-off → `parked` (carried)
- `1cf18f11` F-profilux-orphan-row → `parked` (carried)
- `0e6f3271` F-profilux-reset-link-frozen → `parked` (carried, STATE DO NOT remains against this route)
- `82aaa878` F-screen9-completeness-weight-undocumented → `parked` (carried)
- `5503e29d` F-editor-l1-fallback-education → `open` priority `low` (carried, doc-only caveat)
- `6aef236e` Retire profilux standalone table + dormant `app/[slug]/page.tsx` → `open` priority `low` (carried; Slice 2A `369c2e0` migrated `/p/[slug]` to PublicProjection but `profilux` standalone table retirement still pending)

No new admin_tasks rows created this session.

---

## REPO STATE AT SESSION CLOSE

- HEAD `origin/main` = `b533e31464e19fcebc8581e3cbff0b944305e0b0`
- Uncommitted local changes: STATE rotation pending (this handoff produces it)
- Coolify deploys: S0 docs-only (no rebuild impact), S1 UI rebuild deployed and validated end-to-end in prod
- All commits MCP-verified via `list_commits` immediately after push

---

## DOCTRINE REMINDERS FOR NEXT SESSION OPEN

- Repo truth source order (locked May 7): GitHub MCP → Claude Code local → user paste fallback. Every read declares path + branch + "committed truth, local uncommitted invisible".
- ProfiLux is a single living professional profile object, owned continuously by the user.
- Substrate (`EditorView` §7.6.1, §4.5 W1/W2/W3 write contract, vocabulary, `resolveProfiLux`, `projectFor`) intact.
- MATRIX v1.2 is the supreme implementation contract. v1.1 superseded.
- S1 prod validation reconfirms cv-parse pipeline green. Do not touch unless a new bug surfaces.
- For S1.5: plan only. No code. Mo approves plan before any Code prompt is drafted.

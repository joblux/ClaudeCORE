# HANDOFF — 2026-05-09c

HEAD `3edf6ac`. One refactor slice shipped this session — F-1a — eliminating the parallel `MemberProfile` type system that lived in `types/member-profile.ts` and was consumed only by `app/admin/members/[id]/page.tsx`. The replacement is a canonical `AdminMemberDetail` adapter type in `lib/profilux/types.ts`, formalized as the ONE place `ProfiLuxResolved` is extended for an admin client surface. Zero runtime change, zero rendering change, zero new product behavior. ProfiLux storage / projection / resolver contract unchanged. This was technical-debt cleanup ahead of the recruiter/admin projection convergence work (`C-B-2`, `C-B-3`). Bridge V1.1 second real-use close = THIS close; outcome assessed at next `/joblux-open`.

Doctrine locks added this session: (1) `AdminMemberDetail` is the ONE place `ProfiLuxResolved` is extended for an admin client surface — sibling adapter types required for other surfaces (recruiter share, ATS detail, future `BusinessMemberDetail`); (2) `Omit<ProfiLuxResolved, 'languages'>` pattern formalized for adapters that synthesize stable IDs on L1 passthrough arrays (resolves TS intersection array-element collision); (3) `C-B-2` (admin share-preview) parked pending UX/product decision on client-template visual — no preview code, no scaffolding before the visual is locked by Mo + GPT; (4) parallel client type systems for admin surfaces are dead — future client-shaped types live in `lib/profilux/types.ts` only.

---

## SHIPPED THIS SESSION

- **`3edf6ac`** — F-1a: kill `MemberProfile`, formalize as `AdminMemberDetail` adapter. SHIPPED + PROD-VALIDATED 7/7 (Alex Mason fixture `e6899932` — all admin detail tabs render: Overview, AI Assessment, Experience, Skills, Documents, Preferences, Notes). 5 files, +112/-142 net. Replaces the parallel `MemberProfile` type system (consumed only by `app/admin/members/[id]/page.tsx`) with canonical `AdminMemberDetail` in `lib/profilux/types.ts`. Definition: `AdminMemberDetail = Omit<ProfiLuxResolved, 'languages'> & { full_name, notes, work_experiences[], education_records[], languages[], documents[] }`. The `Omit` on `'languages'` resolves the TS intersection array-element-type collision between `ResolvedLanguage` (L1 passthrough, no `id`) and `AdminLanguage` (route-synthesized stable `id`). Four sub-types added: `AdminWorkExperience`, `AdminEducationRecord`, `AdminLanguage`, `AdminMemberDocument`. Files: `lib/profilux/types.ts` (+76 — adapter + 4 sub-types + doctrine comment), `lib/profilux/index.ts` (+5 — barrel exports), `app/api/admin/members/[id]/route.ts` (+10/-7 — typed `member: AdminMemberDetail` object at JSON response site, no logic change), `app/admin/members/[id]/page.tsx` (+19/-19 — import swap from `@/types/member-profile` to `@/lib/profilux` + 8 type generic replacements; component name `MemberProfilePage` preserved verbatim; all `(member as any).company_name` and `(member as any).org_type` casts preserved as-is for F-2), `types/member-profile.ts` DELETED (114 lines). Build: green on 2nd pass after ONE bounded type-only fix. All 5 conditions of the Step 8 corrective-pass envelope satisfied (scoped file, caused directly by patch, type-only, no new types beyond Step 2, casts preserved). Push exit 0. Coolify deploy: `git_only` verification (pending `F-runtime-build-sha-not-exposed`).

- **Bridge V1.1 second real-use close = THIS close (2026-05-09c)**. Inputs: close-card YAML at `.bridge/relay/close-card.yaml` (precheck exit 0, PyYAML mode), `docs/JOBLUX_STATE.md` (612-line baseline pre-edit), `git log 90567a0..HEAD` (1 commit: `3edf6ac`). Outputs (this slice): STATE rotation per directive (LAST SHIPPED window pinned at 5 — adds `3edf6ac` at top, drops `c84bc39` oldest; DO NOT extended +5 lines with the four F-1a doctrine locks; FINDINGS +3 new entries; PARKED +3 entries `F-2` / `C-B-2` / `C-B-3`; "Last updated" footer rewritten) + this HANDOFF V3 file. No commit by Code. Mo commits manually after diff review.

---

## UNRESOLVED

- **`F-2` business member type reconciliation** *(NEW PARKED)* — the `(member as any).company_name` + `(member as any).org_type` casts in `app/admin/members/[id]/page.tsx` access fields not present on the canonical `members.*` object. Likely a `business_profiles` join or untyped columns. Resolution scope: `business_profiles` schema audit + new `BusinessMemberDetail` SIBLING adapter type (NOT extension of `AdminMemberDetail`) + page-level split between professional and business render paths. Candidate next slice if Mo + GPT pick technical debt over product surface work.

- **`C-B-2` admin share-preview** *(NEW PARKED)* — scoped read-only recruiter-facing share preview surface. Parked pending 5 product/UX decisions: (a) visual style for client template, (b) endpoint α vs β, (c) banner copy, (d) sidebar nav, (e) empty states. No preview code, no scaffolding before the client template visual style is locked by Mo + GPT.

- **`C-B-3` public client-facing page** *(NEW PARKED)* — `/p/[slug]` server-emitted public projection consumer. Gated on share-link doctrine slice: token, expiration, revocation, audit log. Not started.

- **`A2` Manage tab full sharing UX** — gated on `0e6f3271` reset-link unpark. Carries forward unchanged from prior HANDOFF.

- **`F-r-slug-local-types-redeclared`** *(NEW, observation-only)* — `app/r/[slug]/page.tsx` redeclares `WorkExperience` / `EducationRecord` / `MemberLanguage` as LOCAL interfaces inside the public surface. Mirror of the parallel-type pattern killed for the admin surface by F-1a. Future cleanup candidate; defer until `C-B-3` opens since both touch this file — let the `client` projection convergence work resolve both at once instead of double-touching.

- **Bridge V1.1 second real-use observation** — THIS close = the observation. Friction metrics watched quietly: heredoc elimination (this close didn't need it; chat-side emitted only the close-card YAML), Downloads elimination (none used), `.bridge/state` planning utility (no operational mirror needed for this close — no truth probes ran), Code determinism delta (one bounded TS fix matched the spec's allowance exactly), hook noise ceiling (clean). Outcome assessed at next `/joblux-open`. If clean across 1-3 product sessions, `F-close-skill-artifact-friction` → CLOSED.

- **`F-runtime-build-sha-not-exposed`** — carries forward unchanged. Coolify deploy verification on `3edf6ac` stays `git_only`. Mo to verify the dashboard reports green by sight. Real verification still gated on the build-SHA header + `/api/__version` slice (next infra slice post-observation, ~10 LOC of middleware + 1 small route + skill upgrade).

- **All product unresolveds carry forward unchanged from prior HANDOFF (`90567a0`)**: D1 brand correction user-POV map, insider voices publication spine, security findings backlog, salary doctrine repair, WikiLux regen pipeline gap, BIMI. None touched this session — refactor-only. ProfiLux doctrine reaffirmed unchanged: View = living document, Edit = enrichment, Manage = visibility/use status (read-only since `a829033`).

---

## NEXT STRICT STEP

Open next session via `/joblux-open`. Default if Mo + GPT don't override: choose between **`F-2` (business member type reconciliation, technical debt cleanup; pairs naturally with F-1a closure — same file, same pattern, sibling adapter type)** or **wait for client-template visual lock to unblock `C-B-2` (admin share-preview, scoped read-only)**. No third option recommended without explicit pivot.

Posture during the Bridge V1.1 observation window continues: no Bridge V2 / V3 / Away Mode proposals from Claude AI. The operational mirror (`.bridge/state/*.json`) is read-alongside, never authoritative; STATE wins on conflict. Workflow-infrastructure reopen trigger: *"Open JOBLUX workflow infrastructure session — Bridge V2"*. V2 priority queue unchanged: `X-Build-SHA` exposure → real deploy drift probe → possible Away Mode primitives → eventual issue-as-queue (`.bridge/inbox/`).

ProfiLux drift reset phrase if needed: *"living document, not wizard / not submission / not approval / not completion funnel"*. F-1a doctrine reset phrase: *"`AdminMemberDetail` is the one admin-surface adapter — sibling types for everywhere else"*.

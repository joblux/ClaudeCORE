# JOBLUX HANDOFF — 2026-05-16

**Format:** HANDOFF V3 (tactical, 3 sections).
**Replaces:** docs/HANDOFF_2026-05-13-PM.md.

---

## SHIPPED THIS ROTATION

10 commits, 4 chantier packs.

- **26e3032** P1 — section_visibility (real) + masked_fields (substrate). Migration + types + resolver + projectFor public + POST allowlist + Edit per-card PUBLIC toggle + Manage Masked Fields card.
- **09f6b86** Pack A backend — member_languages PUT + sectors CRUD + maisons writable + resolver merge.
- **645ab23** Pack A UI — Languages add-only drawer + Sectors L2 sub-section in Luxury Fit drawer + Maisons SectionCard.
- **cb879c9** Resolver fix — education L2 replaces L1 (kills duplicate-display bug).
- **5ce934e** Pack F — revert PUBLIC ON/OFF toggle from 10 Edit cards. Substrate retained.
- **5db8d59** C.1 cv-merge pending state — cv_parsed_pending column + cv_parse_history table. cv-parse writes pending.
- **99f1f32** C.2 cv-merge diff + reject endpoints (read-only).
- **c2897c2** C.3 atomic apply — apply_cv_merge RPC + 4 UNIQUE expression indexes + apply route + education suggestions race-hardened.
- **f516ccd** C.4 cv-merge review UI — 5 conditional sections with status badges + apply/reject flow.
- **a206fe3** Date normalization fix — partial dates padded server-side before RPC + 500 errors surface in UI.

Live-validated end-to-end on real CV upload (atlas.pdf): pending populated → diff rendered → apply with identity excluded → 5 experiences + 1 education + 1 language + 1 sector inserted, no duplicates, history applied_at set, completeness restored 83%.

---

## UNRESOLVED / PARKED

- **Education suggestions panel re-fires after CV merge** — Apply suggestions card resurrects when L1 swap brings new pending state. Working as designed. Flagged for future UX rationalization, no fix scheduled.
- **`F-completeness-triple-system`** (`f6508e54`) — still parked observation-only. Doctrine fork.
- **Duplicate-apply race** for education suggestions — closed by UNIQUE index + signature-correct lookup at `c2897c2`. No ledger row created; documented in this rotation's STATE LAST SHIPPED entry.
- **C.4 visual proof gap** — first Chrome MCP Apply click raced silently before date fix; validated post-fix via direct fetch + JS dispatch. Real-user click flow needs retest on next session.

---

## NEXT STRICT STEP

**Pack B — Share / Manage finition.** No Pack B execution tonight; tomorrow start.

Pack B scope (items #6, #7, #8, #9, #10, #11, #17 from 17-chantier inventory):
- Share password gate + expiry + view tracking (`share_views` table)
- masked_fields consumers (client share surface + PDF)
- Private + share PDF export (library decision + template)
- Slug rotate/history
- `matching_opt_in` toggle (separate from availability per STATE §20.x)
- Account deletion (cascade) + RGPD export
- Share view tracking consumer (Manage analytics)

After Pack B: Pack D (Add Section library, 8 types) → Pack E (Recruiting loop).

**Pre-session reads:**
1. `app/dashboard/candidate/profilux/page.tsx` Manage tab IIFE — current live state
2. `app/api/profilux/share/route.ts` — current toggle implementation
3. `app/api/profilux/reset-link/route.ts` — current slug logic
4. PDF library decision: `@react-pdf/renderer` vs `puppeteer` vs server-side HTML→PDF
5. Maskable consumers — which surfaces need it first (client share priority; recruiter ATS post-launch)

**Lock for next session:** one prompt per pack unless DB+UI mixed risk forces split (per Mo doctrine 2026-05-16).

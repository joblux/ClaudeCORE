# JOBLUX HANDOFF — 2026-05-12 (Opus session)

## SNAPSHOT

**HEAD:** `d8e6d30` — C1 slice 1 family fully closed.

**Session arc:** Opened in contract closure mode at `b7b9903`. Closed C5 live (Phase A verification), shipped disabled-profile hold page, completed C4 read-only audit, locked C1 architecture, executed all of C1 slice 1 (S-A identity collision detection) end-to-end. Seven commits, all SHIPPED + COOLIFY-GREEN + CHROME-MCP-VALIDATED.

**Next session opens with:** `"Open JOBLUX session — contract closure mode"` → C1 slice S-B Education scalars.

---

## 1. SHIPPED THIS SESSION (verified via GitHub MCP `list_commits`)

Exact count: **7 commits** between session start (`b7b9903`) and HEAD (`d8e6d30`).

| # | SHA | Title | Time UTC |
|---|---|---|---|
| 1 | `2596d8c0` | feat(profilux): add disabled-profile hold page (initial) | 14:18 |
| 2 | `a36866c0` | feat(profilux): strip global chrome on public slug routes + redesign hold page | 14:33 |
| 3 | `edd37f9` | feat(profilux): C1 slice 1A — identity collision detection in resolver predicate | 15:18 |
| 4 | `5497dff` | feat(profilux): C1 slice 1B.1 + 1B.2 — resolution_state plumbing + resolver suppression | 16:04 |
| 5 | `97695ed` | feat(profilux): C1 slice 1B.3 — atomic apply endpoint + UI repoint | 16:19 |
| 6 | `664f293` | feat(profilux): C1 slice 1B.4 — dismiss action + per-row Dismiss UI | 16:32 |
| 7 | `d8e6d30` | feat(profilux): C1 slice 1B.5 — honest panel copy + L2 display on suggestion rows | 16:52 |

### C5 — closed live-verified

Phase A toggle dance executed on `alex-mason` slug via Supabase MCP + curl: 5-cycle toggle dance. OFF → HTTP 404 + `__next_error__` envelope + 0 profile markup. ON → HTTP 200 + profile renders. `unstable_noStore()` from commit `81e3bbd` holds. Ledger `ba8ca121` (F-public-slug-gate-leak) closed via Supabase MCP UPDATE: `status='closed'`, `done=true`, `completed_at=2026-05-12 14:04:59 UTC`, live-verified note appended.

### C5 follow-up — disabled-profile hold page (shipped, good enough)

Two commits (`2596d8c0` replaced by `a36866c0`). Final architecture: `components/layout/LayoutShell.tsx` extends existing skip-chrome pattern (escape/admin/holding) with deny-list of 38 known root segments — any single-segment path NOT in the list = public ProfiLux slug → strip global Header/Footer. New file `app/[slug]/not-found.tsx` renders true standalone full-viewport hold page with bottom-anchored JOBLUX sign-off block (logo image + "Luxury talent intelligence" eyebrow + "Luxury, decoded." italic gold + linked `www.joblux.com`). Copy locked: **"This profile is unavailable."** Accepted scope tradeoff: active public profile also loses global chrome. Ledger `818ccf6b` (public profile not-found polish) closed. Ledger `98219ae5` (V12-divergence-8 active profile legacy layout) kept OPEN as separate slice.

### C4 — read-only audit completed

Audited `lib/profilux/projectFor.ts`, `lib/profilux/types.ts`, `lib/profilux/resolveProfiLux.ts`, plus DB check of `members.profile_visibility`. **5 PASS / 5 GAP / 4 RISK.** Mo accepted state: masking floor exists code-driven, free-text leakage known risk, `profile_visibility` dead/parked, no per-field masking in v1. Q1–Q4 captured in this handoff (NOT ledger) per Mo decision. **C4 closed without ledger noise.**

### C1 — architecture locked + slice 1 family fully shipped

**C1 design decisions (D1–D6 + K1–K6):**
- D1 Hybrid diff (section bulk default, expand to row mode)
- D2 Per-row "Add as ProfiLux entry" for collections; select-all only on clean sections
- D3 Collision = F2 (L1≠L2 suggestions) + F4 (persistent badge)
- D4 Resolution state via `cv_parsed_data.resolution_state` jsonb sub-object
- D5 Stable section order, needs_review pinned within
- D6 NO undo in v1
- K1 One endpoint `POST /api/profilux/suggestions` with `{ action, field, value }`
- K2 Atomic apply (L2 column + resolution_state same UPDATE, error on either failure)
- K3 Per-row dismiss only
- K4 Store value; re-suggest if L1 changes
- K5 Show L2 value on collision rows (shipped in 1B.5 via Option β UI inference)
- K6 Ship 1B.1 + 1B.2 together

**Constraint locked:** prove contract on one narrow section (S-A = identity) before expanding. **S-A identity fully closed.**

**Slice 1 family — all live-verified via Chrome MCP + Supabase MCP:**

| Sub-slice | Commit | Live verification |
|---|---|---|
| 1A — Resolver collision detection | `edd37f9` | Panel renders 2 collision rows for alex-mason (Last name: Mazour, City: New York). first_name matches → no suggestion. nationality both null → no suggestion. |
| 1B.1 — Types + zod plumbing | `5497dff` | `CvParsedDataResolutionItem` + `CvParsedDataResolutionState` types added; cv-parse zod accepts `resolution_state` via `z.any().optional()`. Parser preserves key on re-parse, never writes. |
| 1B.2 — Resolver suppression | `5497dff` | 3-phase live test: baseline (2 sugs), seeded `state.last_name=applied` (last_name suppressed), changed L1 to "Mazouri" (re-fires with new value, K4 contract). |
| 1B.3 — Apply endpoint + UI repoint | `97695ed` | NEW `app/api/profilux/suggestions/route.ts` (176 lines). Path A read-modify-write jsonb. `handleApplySuggestions` repointed to sequential per-field loop (W1). Live: apply last_name → L2=Mazour + state, reload → row suppressed; apply city → both states preserved, panel disappears entirely. |
| 1B.4 — Dismiss action + per-row UI | `664f293` | Extended endpoint to accept `action: 'dismiss'`. UI: killed global Dismiss link, per-row Dismiss button beside value, renamed `applying`/`applyError` → `actioning`/`actionError`, panel-wide disable while in-flight, button label "Working…" for both apply/dismiss. Live: dismiss last_name → L2 unchanged, state=dismissed, row vanishes; dismiss city → panel gone; K4 re-suggest test with L1 "Mazouri" → row re-fires. |
| 1B.5 — Copy correction + L2 display | `d8e6d30` | Panel intro corrected to "Your CV contains values that differ from your ProfiLux. Review each and apply or dismiss." Row value cell renders arrow form `<L2 or (none)> → <L1>` via Option β UI inference (no projection change). Live: collision rows render `Mason → Mazour` / `Paris → New York`; empty-L2 case renders `(none) → French`. |

**Live fixture:** alex-mason owner = `mzaourmohammed@gmail.com` (member id `49542211-fe29-4833-a141-eb1eaa7d248f`). Baseline restored after every verification: L2 Mason/Paris, L1 Mazour/New York, no resolution_state, score 49.

**Doctrinal lock:** L1 → L2 silent writes remain forbidden across all code paths. CV merge identity contract is now fully explicit-confirmation, with re-suggestion suppression honoring applied + dismissed status per K4.

---

## 2. C4 Q1/Q3/Q4 — PARKED OBSERVATIONS (handoff doc only, NOT ledger)

Captured during C4 audit. Carried forward to next session.

- **Q1 — Per-field masking gates C7.** No jsonb maskable-field schema exists. Masking is code-driven not data-driven. C7 (manage projection/preferences) cannot ship user-controllable per-field masking until this schema is decided. **Disposition:** open question for C7 design phase.
- **Q3 — Free-text leakage risk.** `PublicExperience.description`, `headline`, and `bio` are free-text passthroughs. They CAN leak V3/V4 brand info and current_employer signal that the structural masks otherwise enforce. **Disposition:** open question for C7 / future hardening slice. No fix scheduled.
- **Q4 — Retire dead `profile_visibility` column.** `members.profile_visibility` is dead: default `'team_only'`, 12/12 rows hold default, never read in `projectFor`. Candidate for DDL drop, but doctrine ties this to broader privacy schema design (Q1). **Disposition:** retire as part of C7 schema design, OR park as F-dead-column-cleanup.

**Q2 was answered:** CV merge staging architecture is L1 raw payload (cv_parsed_data) → L2 applied (members columns) → resolution_state jsonb sub-object. projectFor unchanged. **DONE.**

---

## 3. CURRENT STEP — NEXT SESSION ENTRY

**Trigger:** `"Open JOBLUX session — contract closure mode"`

**Strict next step:** Continue C1 per locked sequence: **S-B Education scalars.**

### S-B Education scalars — read-first scope

Identity used `members.first_name`, `members.last_name`, `members.city`, `members.nationality` — 4 flat scalar columns. Education has the same shape via 3 flat scalars: `members.university`, `members.field_of_study`, `members.graduation_year`.

The S-A contract should clone cleanly with field-name swap. **But verify before implementing:**

1. Confirm `cv_parsed_data.education` (the L1 shape) is a flat scalar trio OR a collection. If collection (likely an array of education records), the same per-row collision/apply/dismiss machinery is required and 1B becomes more complex.
2. Confirm `editor.university` / `editor.field_of_study` / `editor.graduation_year` types in `EditorView`.
3. Confirm pickSuggestion is currently only wired for 4 identity fields. The eligibility predicate generalizes, but call sites do not.

**Constraint:** do NOT touch `lib/profilux/resolveProfiLux.ts` experiences merge logic (A2.3-β.2 lock at `351421f` — L2 editable rows + L1 parsed CV rows simultaneously visible, no dedup, no silent L1→L2 promotion). Same pattern applies if S-B Education turns out to be collection-shaped.

### After C1 S-B closure

Locked C1 sequence continues: **S-C Experiences full collection** (requires `351421f` doctrine compatibility check) → **S-D Sectors** (gated on `1609e494` parking decision).

After C1 family complete: continue locked closure order C2/C3/C8 → C6 → C7.

---

## 4. PARKED / OPEN LEDGER ROWS

Still open (carried forward):

- `98219ae5` — V12-divergence-8: public profile `/[slug]` legacy layout vs converged View doctrine. **Separate slice from hold page work.** Deferred until contract closure phase completes.
- `3e8d6de2` — V12-divergence-4 (deferred)
- `d243fc13` — V12-divergence-5 (deferred)
- `39117b15` — F-public-profile-sitemap-audit (deferred)

Closed this session:
- `ba8ca121` — F-public-slug-gate-leak — CLOSED live-verified (Phase A toggle dance, 5 cycles).
- `818ccf6b` — Public profile not-found polish — CLOSED via `a36866c0`.

C1 slice 1 family was not pre-ledgered; tracked in this handoff only.

---

## 5. DO NOT (carried forward, additions from this session)

Additions to STATE §`### DO NOT`:

- Do not touch `app/api/profilux/suggestions/route.ts` outside of new slices in the C1 family. The endpoint contract is locked: `{ action: 'apply' | 'dismiss', field: <identity_key>, value: string }`. Adding new actions, fields, or response shapes requires explicit Mo approval.
- Do not reintroduce a global "Dismiss all" button to the S1.5 panel. K3 contract = per-row dismiss only.
- Do not touch `LayoutShell.tsx` skip-chrome deny-list without confirming the candidate single-segment path is or is not a public ProfiLux slug. False positive = chrome stripped from a real top-level page. False negative = chrome wraps a public profile.
- Do not change the arrow form rendering `<L2 or (none)> → <L1>` to use `Currently:` / `CV says:` dual labels without an explicit slice. Option β (UI inference) is locked; no projection field for raw L2 is needed.
- Do not let `editor[k] === sug[k]` reasoning leak into the resolver. The inference lives in `app/dashboard/candidate/profilux/page.tsx` only.
- Do not write to `resolution_state` from any code path other than `/api/profilux/suggestions`. The CV parser preserves the key on re-parse but never writes it.

---

## 6. WORKFLOW DOCTRINE — REINFORCEMENTS

From session execution:

- Mo = relay between Claude AI (Opus, this) and Claude Code (terminal executor). GitHub MCP write blocked at OAuth scope. Mo pastes prompts, Mo signals green, then Claude AI runs prod QA directly via Supabase MCP + Chrome MCP. Mo does NOT do prod QA.
- Read-first audits are MANDATORY before any implementation prompt. 8-gate audit pattern (a)–(h) is now the standard. Every audit gate must be a verifiable PASS/FAIL, not a vibe.
- Smoke check before commit owned by Claude Code (typecheck + file scope + grep sanity). Claude AI cannot see uncommitted local truth.
- "No copy/visual changes" interpreted strictly. Accurate state-labeling (e.g. "Working..." for shared apply/dismiss in-flight) is NOT copy polish; it is correctness.
- C1 architecture lock proved its value. Without D-K decisions pre-committed, slice drafting would have spiraled. Pre-committed decisions are non-negotiable for future contract families.
- Live verification via Chrome MCP + Supabase MCP is fast enough to run after every deploy (multiple per session today). Do not skip even on "obvious" changes.

---

**Last updated:** 2026-05-12 (Opus session) — C5 closed live-verified + hold page shipped + C4 audit completed + C1 architecture locked + C1 slice S-A (identity) fully closed via 7 commits. Next session opens at S-B Education scalars.

— Claude AI (Opus)
